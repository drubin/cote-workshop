module:
  description: redis service
  type: container
  name: redis
  image: redis:5.0.2-alpine
  services:
    - name: redis
      ports:
        - name: redis
          protocol: TCP
          containerPort: 6379
